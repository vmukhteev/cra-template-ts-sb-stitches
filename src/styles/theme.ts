// import { ITokensDefinition } from '@stitches/core/dist/declarations/src/types';

const theme = {
  space: {
    $1: '5px',
    $2: '10px',
  },
  fontSizes: {
    $1: '12px',
    $2: '20px',
    $3: '30px',
  },
} as const;

export default theme;
